const express = require("express");
const app = expres();

app.get()
app.post()
app.post()
app.delete()


app.get("/", (req, res) =>
{
    res.send("Hello world");
});

app.listen(3000, () => console.log("listening on port 3000"));
